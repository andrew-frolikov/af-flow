# Xcode
build/
DerivedData/
*.xcuserstate
xcuserdata/
.codex/

# Models (large binary files)
Resources/models/
GhostPepper/Resources/models/

# Secrets
.env
.env.*
Config/LocalSigning.xcconfig
# AF Flow: Secrets.swift is intentionally NOT ignored. It must never exist
# (hard rule 1); un-ignoring it makes any accidental creation visible in git status.

# macOS
.DS_Store

# Swift Package Manager
.build/
.swiftpm/

# Profiling/coverage data
*.profraw
*.profdata

# Logs
*.log

# Local user data / generated private artifacts
**/Ghost Pepper Meetings/
/.indexes/
/wikis/
/Reads/
/20??-??-??/
**/transcription-lab/
transcription-lab-index.json
transcription-lab-timings.json
debug-log.json
cache-v6.json
cache-v6.json.enc
*.jsonl
*.wav
*.m4a
*.mp3
*.caf
*.aiff
*.mov
*.mp4
*.webm
screenshots/
Screenshots/

# Only keep shipping-facing docs/assets in git.
docs
!docs/
docs/*
!docs/index.html
!docs/pre-deploy-privacy-security.md
.superpowers/
inspo/

# Local-only experiments that require an unsandboxed dev build
GhostPepper/GhostPepper-Dev.entitlements
GhostPepper/Meeting/GranolaSessionAuth.swift
GhostPepper/Meeting/GranolaSessionSync.swift

# AF Flow session handoffs
.handoff/

# Fixture reference texts are Andrew's real unscripted speech, not a script.
# The audio is already excluded above; the transcripts are the same content.
fixtures/*.reference.txt
fixtures/*.draft-reference.txt
fixtures/*.hypotheses.json
fixtures/scores.md
